<!DOCTYPE html>
<html>
<head>
  <title>Prime Directive</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <meta name="viewport" content="width=device-width">
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="row">
      <div class="large-12 columns">
        <div id="logo">
          <%= link_to root_path do %>
            <h1>Prime Directive</h1>
          <% end %>
        </div>
        <% flash.each do |key, value| %>
          <div class="alert-box">
            <div class="flash <%= key %>"><%= value %></div>
            <a href="" class="close">&times;</a>
          </div>
        <% end %>
      </div>
    </div>
  </header>
  <%= yield %>
  <div class="row email signup">
      <%= form_for :newsletter_request, url: newsletter_requests_path do |f| %>
        
        <div class="large-8 columns">
          <%= f.text_field :email, :placeholder=> "E mail", :class=> "inline"  %>
        </div>
        <div class="large-4 columns">
          <%= f.submit "Get our best weekly gift ideas!", :class=>"button prefix" %>
        </div>
      <% end %>
  </div>

</body>
</html>
